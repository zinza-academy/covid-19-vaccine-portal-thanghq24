'use client';
import Header from '@src/components/sharedComponents/PageHeader/Header';
import { FC } from 'react';

const Home: FC = () => {
  return (
    <main>
      <Header />
    </main>
  );
};

export default Home;
